{
  "_comment": "VS Code (GitHub Copilot) — 放置于项目 .vscode/mcp.json 或用户 settings.json。注意 VS Code 用的是 servers 而非 mcpServers",
  "_install": "推荐: pip install -e D:/AndroidMEM/AndroidMiniMem/mcp，然后 command 改为 minimem-mcp 并删除 args",
  "servers": {
    "minimem": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "minimem_mcp"],
      "env": {
        "PYTHONPATH": "D:/AndroidMEM/AndroidMiniMem/mcp",
        "MINIMEM_IPC_HOST": "127.0.0.1",
        "MINIMEM_IPC_PORT": "28100"
      }
    }
  }
}
