{
  "_comment": "VS Code (GitHub Copilot) — 放置于项目 .vscode/mcp.json 或用户 settings.json。注意 VS Code 用的是 servers 而非 mcpServers",
  "_install": "推荐: pip install -e D:/AndroidMEM/AndroidMiniMem/mcp，然后 command 改为 amem-mcp 并删除 args",
  "servers": {
    "amem": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "amem_mcp"],
      "env": {
        "PYTHONPATH": "D:/AndroidMEM/AndroidMiniMem/mcp",
        "AMEM_IPC_HOST": "127.0.0.1",
        "AMEM_IPC_PORT": "28100"
      }
    }
  }
}
