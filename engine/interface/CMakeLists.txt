# ============================================================================
# 接口模块构建配置（MiniMem 仅保留 Socket 接口）
# ============================================================================

if(BUILD_SOCKET_INTERFACE)
    add_subdirectory(socket)
endif()
